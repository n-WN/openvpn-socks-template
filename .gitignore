# Node/editor/OS
.DS_Store
*.swp

# Environment (may contain secrets)
.env

# Runtime
*.log

# Avoid committing real VPN materials; keep sanitized sample only (handled in config/.gitignore)
# Avoid accidental auth leakage
config/auth/

# Do not commit local notes
docs/

# Never commit local real keys
key/
