This directory is intentionally empty. Place your real OpenVPN materials here:
- client.ovpn (the connection profile)
- ca.crt, client.crt, client.key (if referenced by the .ovpn)
Do not commit your real materials. They are ignored by .gitignore.
